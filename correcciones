quitar:

- Diagrama de clases
- Mucho texto..
- Cambiar CQL de tabla por Accessor:
                        @Query("SELECT * FROM picture WHERE id = :id")
                        Picture findOne(@Param("id") UUID id);

                        @Query("SELECT * FROM picture WHERE solr_query = :query LIMIT :total")
                        Result<Picture> findAllOrdered(@Param("query") String query, @Param("total") int total);

                        @Query("SELECT * FROM picture LIMIT 10000")
                        Result<Picture> findAll();

                        @Query("SELECT * FROM picture WHERE owner = :owner LIMIT 10000")
                        Result<Picture> findByOwner(@Param("owner") String owner);

                        @Query("SELECT * FROM picture WHERE solr_query = :query")
                        Result<Picture> search(@Param("query") String query);

- Implementación:
   Puntos del proyecto web: MVC

- ¿Poner código? Hay dos ejemplos de Lire como index y como busca.

- Clarear la imagen de Gracias
